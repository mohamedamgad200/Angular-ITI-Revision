import { HighligtdirectiveDirective } from './highligtdirective.directive';

describe('HighligtdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligtdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
