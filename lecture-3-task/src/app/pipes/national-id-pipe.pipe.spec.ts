import { NationalIdPipePipe } from './national-id-pipe.pipe';

describe('NationalIdPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NationalIdPipePipe();
    expect(pipe).toBeTruthy();
  });
});
